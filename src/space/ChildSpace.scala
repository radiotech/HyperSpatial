package space

object ChildSpace {
  def apply(objs: List[Obj], pts: List[List[Pt]]) = new ChildSpace(objs,pts)
}

class ChildSpace(val objs: List[Obj], val pts: List[List[Pt]]) {
  /*
   * crush(FOV)
   * 
   */
  
  
  
  
}